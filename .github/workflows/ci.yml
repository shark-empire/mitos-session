name: TEST MITOS session

on: [push, pull_request]

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y libpam0g-dev libclang-dev
      - run: cargo check --all-targets --all-features

  build:
    name: build
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install libclang
        run: sudo apt-get update && sudo apt-get install -y libclang-dev
      - run: cargo build --all-targets --verbose
      # Also validate the size-optimized [profile.release] in Cargo.toml
      # (LTO + panic=abort can surface issues a debug build won't).
      - run: cargo build --release --verbose

  test:
    name: test
    runs-on: ubuntu-latest
    needs: check
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install libclang
        run: sudo apt-get update && sudo apt-get install -y libclang-dev
      - run: cargo test --all-targets --verbose
